import React from 'react'
import PDF from './PDF'

const Research = (isDarkMode) => {
  
return ( 
<div className="bg-gray-500 py-8">
<PDF isDarkMode={isDarkMode}/>
</div>

 ) 
}

export default Research
